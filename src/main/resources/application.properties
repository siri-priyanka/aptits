# ====== Database Configuration ======
spring.datasource.url=jdbc:mysql://nowdb-1.c7wsisw447ne.ap-south-1.rds.amazonaws.com:3306/tomdb
spring.datasource.username=admin
spring.datasource.password=Uz6KLIzrJNpzxY4oXlLj
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ====== Tomcat WAR Deployment Settings ======

# Remove this if present: embedded port (Tomcat controls it)
# server.port=8080

# Optional: to access at http://<ip>:8080/aptits/
server.servlet.context-path=/aptits

# ====== Thymeleaf (optional) ======
spring.thymeleaf.cache=false

# ====== Actuator (optional) ======
management.endpoints.web.exposure.include=*

# ====== Logging (optional) ======
logging.level.org.springframework=INFO
